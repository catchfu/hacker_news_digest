name: Hacker News Digest

on:
  schedule:
    - cron: '0 0 * * *'  # Daily at midnight UTC
  workflow_dispatch:
    inputs:
      period:
        description: 'Period (e.g., 24h, 72h)'
        required: false
        default: '24h'
      articles:
        description: 'Number of articles per category'
        required: false
        default: '5'
      send_email:
        description: 'Send email'
        required: false
        default: 'true'

jobs:
  digest:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build
        run: npm run build
        
      - name: Generate digest
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: |
          if [ "${{ github.event.inputs.period }}" != "" ]; then
            npm start -- --period ${{ github.event.inputs.period }} --articles ${{ github.event.inputs.articles }} --send-email
          else
            npm start -- --send-email
          fi
          
      - name: Upload digest artifact
        uses: actions/upload-artifact@v4
        with:
          name: digest
          path: digest-*.md
          retention-days: 30
